apiVersion: v1
kind: Service
metadata:
  name: "{{ template "dionysos.uname" . }}"-service
spec:
  selector:
    app: "{{ template "dionysos.uname" . }}"
  ports:
    - name: "http"
      port: {{ .Values.envVariables.dionysos.port }}
      targetPort: {{ .Values.envVariables.dionysos.port }}
