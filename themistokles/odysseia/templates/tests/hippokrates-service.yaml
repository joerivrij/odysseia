apiVersion: v1
kind: Service
metadata:
  name: hippokrates
spec:
  selector:
    app: hippokrates
  ports:
    - name: "http"
      port: {{ .Values.envVariables.ploutarchos.port }}
      targetPort: {{ .Values.envVariables.ploutarchos.port }}
